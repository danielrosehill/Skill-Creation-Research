# Copy to .env and fill in values to enable the bundled MCP servers.
# The project works without these — MCP servers will simply fail to start
# if their required env vars are missing, and Claude will continue using
# other tools.

# Tavily web search — https://app.tavily.com/
TAVILY_API_KEY=
